name: concurrency-1
on:
    workflow_dispatch: 
        inputs:
            environment:
                type: environment
                required: true
concurrency: 
    group: concurrency-1                
    cancel-in-progress: true
jobs:
    sleep-job:
        runs-on: ubuntu-latest
        steps:
            - run: sleep 30               